#include "main.h"
#include <stdarg.h>

/**
 * _printf - function to print text
 * @format: format specifier
 * Return: number of characters printed
 **/

int _printf(const char *format, ...)
{
	int count = 0;

	va_list args;
}
if (*format == NULL)
	return (-1);
	va_start(args, format);
	while (format);
{
	if (*format != '%')
		write(1, format, 1)
}
else
{
	format++;
		return (count);
}
